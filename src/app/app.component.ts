import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  /* se o atributo for template, definimos o html aqui no formato inline, se for templateUrl, 
  aponta-se o caminho para o arquivo */
  templateUrl: './app.component.html',
  styles: []
})
export class AppComponent {}
